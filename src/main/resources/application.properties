spring.application.name=Cafeteria Soma

# 🗄️ CONFIGURACIÓN BASE DE DATOS LOCAL
spring.datasource.url=jdbc:mysql://localhost:3306/cafeteria_soma?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Antony03
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Fuerza la inicialización de la base de datos con schema.sql y data.sql
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true


# 🧩 CONFIGURACIÓN JPA / HIBERNATE
# Crea o actualiza automáticamente las tablas según tus entidades
spring.jpa.hibernate.ddl-auto=update

# Muestra las consultas SQL generadas en consola
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Dialecto para MySQL 8.x
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect


# ⚙️ CONFIGURACIÓN GENERAL DEL SERVIDOR
server.port=8080
spring.application.name=cafeteria-soma


# 🔐 SEGURIDAD (modo desarrollo)
# Usuario y contraseña por defecto (temporal)
spring.security.user.name=admin
spring.security.user.password=admin123
spring.security.user.roles=ADMIN


# 💾 HERRAMIENTAS DE DESARROLLO
# Permite recargar automáticamente cambios (si tienes DevTools)
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true

# =========================================
# 🔄 COMPORTAMIENTO AL INICIAR EL PROYECTO
# =========================================
# Si la base no existe, la crea.
# Si las entidades cambian, actualiza las tablas automáticamente.
# No borra datos existentes (a diferencia de "create-drop").
# Ideal para desarrollo local y trabajo colaborativo inicial.
